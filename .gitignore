# Gradle / Android generated files
.gradle/
build/
*/build/
local.properties
.idea/
*.iml
app/build/
.cxx/
.externalNativeBuild/

# Native generated binaries only (keep hostbuild/*.cpp tests tracked)
hostbuild/native/
hostbuild/*.o
hostbuild/*.a
hostbuild/*.so
hostbuild/test_m58_foundation

# Local signing credentials
*.jks
*.keystore

# CI reports
tmp/
